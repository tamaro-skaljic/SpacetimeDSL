[package]
name = "spacetimedsl-debug"
version = "1.0.0"
edition = "2024"
publish = false

[dependencies]
colored.workspace = true
proc-macro2 = { workspace = true, features = ["span-locations"] }
syn = { workspace = true, features = [
    "extra-traits",
    "full",
    "parsing",
] }
