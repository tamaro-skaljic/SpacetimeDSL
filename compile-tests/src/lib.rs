//! Deliberately empty.
//!
//! This crate exists only for its `tests/` directory, where `trybuild` compiles the
//! table definitions in `tests/ui` and compares the resulting compiler errors against the
//! `.stderr` file beside each one. See `tests/compile_tests.rs`.
